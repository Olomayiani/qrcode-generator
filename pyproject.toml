[project]
name = "qr-code-generator"
version = "0.1.0"
description = "Qr code generator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pylance>=1.0.0",
    "qrcode[pil]>=8.2",
]
